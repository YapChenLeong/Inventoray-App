<!-- res/drawable/border_button.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/colorContainer" />
            <stroke
                android:width="0.5dp"
                android:color="@color/colorContainer" />
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="@color/colorContainer2" />
            <stroke
                android:width="0.5dp"
                android:color="@color/colorContainer" />
        </shape>
    </item>
</selector>